CREATE TABLE phone_to_device (
               			       number text,
                    	       deviceid text,
                    	       active boolean,
                               PRIMARY KEY (number)
                             ); 