CREATE TABLE phone_numbers (
               		 	      number text,
                    	      device_id text,
                    	      active boolean,
                              PRIMARY KEY (number)
                           ); 