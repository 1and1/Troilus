CREATE TABLE phonenumbers (
               			    number text,
                    	    deviceid text,
                    	    active boolean,
                            PRIMARY KEY (number)
                          ); 